# Distributed Architecture Requirements
# Add these to your existing requirements.txt

# Redis for distributed caching and job queue
redis>=5.0.0
hiredis>=2.2.0  # C parser for better performance

# PostgreSQL for persistent storage (replaces SQLite)
psycopg2-binary>=2.9.0
asyncpg>=0.29.0

# Connection pooling
sqlalchemy[asyncio]>=2.0.0

# Process management
supervisor>=4.2.0

# Monitoring and metrics
prometheus-client>=0.19.0
sentry-sdk>=1.40.0

# Load balancing (optional)
gunicorn>=21.2.0
uvicorn>=0.27.0
